# Vite frontend environment configuration
# Set this to the backend API URL in development or production.
# For same-host deployment, use /api to keep frontend and backend together.
VITE_API_URL=/api

# In local development, override with the backend address if needed:
# VITE_API_URL=http://localhost:9999/api
